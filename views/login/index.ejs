<!DOCTYPE html>
<html lang="en">
  <%- include ("../partials/head") %>

  <body>
    <h1 class="text-center py-5">&#x1F6E1; Gatekeeper - Login &#x1F6E1;</h1>

    <div class="container">
      <form method="post" style="max-width: 400px;" class="mx-auto">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <div class="form-group">
          <label for="loginEmail">Email</label>
          <input
            type="email"
            class="form-control <%= locals.emailError ? 'is-invalid' : null%>"
            id="loginEmail"
            name="email"
            value="<%= locals.emailPrefill ? locals.emailPrefill : null %>"
          /> <!--Input name must be "username" for passport authentication strategy-->
          <% if (locals.emailError) { %>
            <div class="invalid-feedback"><%= locals.emailError %></div>
          <% } %>
        </div>
        <div class="form-group">
          <label for="loginPassword">Password</label>
          <input
            type="password"
            class="form-control"
            id="loginPassword"
            name="password"
          />
        </div>
        <div class="text-center">
          <button type="submit" class="btn btn-primary text-center mx-auto">Submit</button>
        </div>
        <div class="text-center my-3">
          - OR -
        </div>
        <div class="oauth-login-btns text-center">
          <a class="btn btn-primary google-login-btn" href="/auth/login/google?r=<%= locals.redirectLink %>">
            <i class="fab fa-google"></i>
            Login with Google
          </a>
          <a class="btn btn-primary facebook-login-btn" href="/auth/login/facebook?r=<%= locals.redirectLink %>">
            <i class="fab fa-facebook-f"></i>
            Login with Facebook
          </a>
        </div>
      </form>
    </div>

  <%- include ("../partials/scripts") %>
  </body>
</html>
